#include "Transform.h"
#include "Entity.h"

namespace primal::transform {
	namespace {
		utl::vector<DirectX::XMFLOAT3> position;
		utl::vector< ? ? ? > rotation;
		utl::vector< ? ? ? > scale;
	} // anonymous namespace
}